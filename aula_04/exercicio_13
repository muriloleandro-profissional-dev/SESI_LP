#include <stdio.h>

int main(){
    
    int i = 0, soma = 0;
    
    for(i = 0; i <= 100; i++){
        
        if(i % 2 != 0 ){
            soma = soma +i;
        }
    }
    
    printf("A soma dos números ímpares de 1 a 100 é %i", soma);
    
}