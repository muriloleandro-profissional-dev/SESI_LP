#include <stdio.h>

int main() {
    int numero, soma = 0;

    do {
        printf("Digite um número (0 para sair): ");
        scanf("%i", &numero);

        soma = soma + numero;

    } while(numero != 0);

    printf("A soma de todos os números digitados é: %i\n", soma);

    return 0;
}