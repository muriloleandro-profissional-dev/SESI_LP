#include <stdio.h>

int main(){
    
    int matriz[3][3];
    int soma_linha=0, soma_linha_2=0, soma_linha_3=0;
    
    
    for(int l=0; l < 3; l++){
        
        for(int c=0; c < 3; c++){
            
            printf("Número da linha %i:  e coluna %i: ", l+1, c+1);
            scanf("%i", &matriz[l][c]);
        }
        

        
    }

    for(int l=0; l < 3; l++){
        
        for(int c=0; c < 1; c++){
            
            for(int i=0; i < 3; i++){
                
                printf(" | %i ", matriz[l][c]);
                
            }
            
            soma_linha = soma_linha + matriz[0][c];
            soma_linha_2 = soma_linha_2 + matriz[1][c];
            soma_linha_3 = soma_linha_3 + matriz[2][c];

        }

        
        printf(" \n");
        
    }

    printf("\n Soma da linha 1: %i", soma_linha);
    printf("\n Soma da linha 2: %i", soma_linha_2);
    printf("\n Soma da linha 3: %i", soma_linha_3);
    
    
}
